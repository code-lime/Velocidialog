package com.velocitypowered.proxy.protocol;

/** Minimal outbound-state names used by the Velocity 4 runtime fixture. */
public enum StateRegistry {
    HANDSHAKE,
    STATUS,
    LOGIN,
    CONFIG,
    PLAY
}
