package com.velocitypowered.proxy.protocol;

/** Minimal outbound-state names used by the Velocity runtime fixture. */
public enum StateRegistry {
    HANDSHAKE,
    STATUS,
    LOGIN,
    CONFIG,
    PLAY
}
